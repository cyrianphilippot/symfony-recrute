{% extends '::admin/index.html.twig' %}


{% block title %}
    Accueil - {{ parent() }}
{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-sm-12">
            <h4 class="pull-left page-title">Administration</h4>
            <ol class="breadcrumb pull-right">
                <li><a href="#">Administration</a></li>
                <li><a href="#">Show Skill</a></li>
                <li class="active">{{ skill.name }}</li>
            </ol>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Skill</h3>
                </div>
                <div class="panel-body">
                    {# On affiche tous les messages flash dont le nom est « info » #}
                    {% for message in app.session.flashbag.get('notice') %}
                        <div class="alert alert-success">Message flash : {{ message }}</div>
                    {% endfor %}

                    <div class="row">
                        <div class="col-md-12 col-sm-12 col-xs-12">
                            <table class="table table-striped">
                                <tbody>
                                <tr>
                                    <th>Id</th>
                                    <td>{{ skill.id }}</td>
                                </tr>
                                <tr>
                                    <th>Name</th>
                                    <td>{{ skill.name }}</td>
                                </tr>

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div> <!-- End Row -->

    <ul>
        <li>
            <a class="btn btn-default" href="{{ path('admin_skill_index') }}">Back to the list</a>
        </li>
        <li>
            <a class="btn btn-primary" href="{{ path('admin_skill_edit', { 'id': skill.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
